# Copy this file to `.env` and fill in secrets. Do NOT commit `.env` to git.

# ntfy configuration (messages to print)
NTFY_HOST=https://ntfy.example.com
NTFY_TOPIC=my-topic
COUNTRY_CODE=1        # For US
#COUNTRY_CODE=44       # For UK
#COUNTRY_CODE=33       # For France

# Phone Number QR Code Feature (auto-converts phone numbers to tel: or sms: schemes)
# Enable this feature to automatically transform numeric click URLs based on message keywords
PHONE_QR_ENABLED=true

# Keywords that trigger CALL (tel: scheme) - comma-separated, case-insensitive
PHONE_CALL_KEYWORDS=call

# Keywords that trigger TEXT/SMS (sms: scheme) - comma-separated, case-insensitive
PHONE_TEXT_KEYWORDS=text,message

# Logging configuration
LOG_LEVEL=INFO
LOG_FILE=/var/log/receipt-printer.log

# Error notifications (separate topic from NTFY_TOPIC above!)
# This sends errors TO a different ntfy topic for monitoring/alerts
# ERROR_NTFY_TOPIC=https://ntfy.sh/my-printer-errors



# Auto-update configuration
AUTO_UPDATE=false
UPDATE_CHECK_INTERVAL=3600
GITHUB_REPO=VoidLock/RecieptPi

# USB printer IDs (optional overrides)
PRINTER_VENDOR=0x0fe6
PRINTER_PRODUCT=0x811e
# Optional printer profile name for python-escpos (e.g. RP80, TM-T20)
# See https://mike42.me/escpos-printer-db/ for supported models and profiles
# PRINTER_PROFILE=

# Paper geometry (80mm paper has 72mm printable width, 58mm paper has 48mm printable width)
PAPER_WIDTH_MM=80

# Centering offsets (mm). Negative X shifts left, positive X shifts right.
X_OFFSET_MM=0
Y_OFFSET_MM=0

# Memory safeguards
MEM_THRESHOLD_PERCENT=80
MEM_RESUME_PERCENT=70
MAX_MESSAGE_LENGTH=300
MAX_LINES=3

# === OPTIONAL: Image output compatibility ===
# IMAGE_IMPL sets one implementation (default: bitImageColumn)
# IMAGE_IMPLS lets you try a list in order, e.g. bitImageColumn,bitImageRaster,graphics,raster
# IMAGE_IMPL=
# IMAGE_IMPLS=
# IMAGE_SCALE=2
# IMAGE_CONTRAST=2.0
